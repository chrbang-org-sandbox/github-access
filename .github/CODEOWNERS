# Everything that controls access is owned by the platform team. The ruleset on main requires approval from a code owner,
# so no PR against these files can be merged without someone in platform having seen it. .github/ is included because
# whoever can change the workflow files can use the App token.
access.yaml   @chrbang-org-sandbox/platform
clients.yaml  @chrbang-org-sandbox/platform
audit.sh      @chrbang-org-sandbox/platform
tools/        @chrbang-org-sandbox/platform
.github/      @chrbang-org-sandbox/platform
